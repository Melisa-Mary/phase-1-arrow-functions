// Function expression (not arrow function)
const divide = function() {
  return 2000 / 100;
};

// Arrow function: squares a number
const square = x => x * x;

// Arrow function: adds two numbers
const add = (a, b) => a + b;
